# Copy this to aggregation/.env and fill in your values.
# aggregation/.env is gitignored - never commit it.
#
# On Narval: place this file in the repo before submitting the job.
# The SLURM script sources it so the values are available to the process.

WANDB_API_KEY=your_key_here
WANDB_ENTITY=your_entity_here
WANDB_PROJECT=jersey-aggregation
